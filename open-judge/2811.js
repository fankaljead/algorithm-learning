// http://bailian.openjudge.cn/practice/2811/
// https://oi-wiki.org/basic/enumerate/

function printArr(arr = []) {
  arr.forEach((nums) => console.log(nums.join(" ")));
}

function solution(arr = [[]]) {
  let n = arr.length,
    m = arr[0].length;

  for (let i = 0; i < n; ++i) {
    for (let j = 0; j < m; ++j) {

    }
  }

  printArr(arr);
}
